# Notas — Rodada 10

## Requisito do usuário (mensagem atual)
1. Atualizar a habilidade vyroscope-video-analyst (skill-creator) com todo o processo usado até aqui. A skill JÁ EXISTE em /home/ubuntu/skills/vyroscope-video-analyst (criada na rodada 4, validada). Preciso ATUALIZÁ-la com o processo completo (análise de nicho, roteiro estendido, comparador, agenda, thumbnails IA, monitoramento, títulos alternativos, favoritos com pastas, ideia do dia, esboço, exportações PDF/CSV).
2. PDF de favoritos: incluir título sugerido associado a cada thumbnail na exportação.
3. Favoritos: contador de thumbnails por pasta atualizado em tempo real durante ações em lote.
4. Favoritos: atalho Ctrl+A para selecionar todas as thumbnails na galeria.
5. Testes vitest + screenshots + checkpoint.

## Estado atual (fase 2, frontend+backend)

### 2.1 Títulos no PDF (buildFavoritesPdf em server/exportPdf.ts)
- O tipo FavoritesExportRow.thumbnail já tem suggestionTitle (título da sugestão gerada pela IA, linha 9 de exportPdf.ts).
- O card atual desenha: imagem + ordem manual + título pequeno cinza claro (line 286-289).
- MELHORIA: adicionar uma linha "SUGESTÃO ASSOCIADA" em âmbar com o título destacado abaixo do card (linha 287 usa o suggestionTitle mas pequeno). Reforçar: label "SUGESTÃO ASSOCIADA" (COLORS.amber, characterSpacing 2) + título em light Helvetica 9.
- buildExportRows no Favorites.tsx (mapThumb) já passa suggestionTitle. Nada a mudar no frontend além de conferir.

### 2.2 Contador por pasta em tempo real
- countInFolder(id) = items.filter(r => r.suggestion_thumbnails.folderId === id).length — já recalcula a partir do cache items (que recebe otimista update no moveMutation: o cache é atualizado { folderId } — SIM, o toggleMutation/moveMutation atualizam o cache otimista).
- PROBLEMA potencial: a pill "Todas (items.length)" e as pills usam countInFolder — já são reativas. Em ações em lote (handleBatchMove) o moveMutation.mutate é chamado N vezes; o otimista update já reflete. Verificar que a UI re-renderiza: itens.filter usa items (do cache), ok.
- A barra de seleção precisa limpar seleção após move (já limpa). OK.
- Adicionar contador na pill "Nova pasta"? Não. Apenas garantir pills mostram contadores corretos.

### 2.3 Atalho Ctrl+A na página Favoritos
- useEffect com listener keydown: se (e.ctrlKey || e.metaKey) && e.key === "a" e alvo é a página (não input/textarea), preventDefault + selectAllFiltered.
- Importar useEffect (verificar import atual: linha ~53 tem `import { useState } from "react";` — adicionar useEffect).

### 2.4 Skill (fase 3)
- Skill existente: /home/ubuntu/skills/vyroscope-video-analyst (ver SKILL.md antes de editar).
- Skill-creator: usar /home/ubuntu/skills/skill-creator/scripts/quick_validate.py ao final.
- Atualizar SKILL.md + possivelmente references/ com: prompt de análise (YouTube Data API search+videos, top 10-15 vídeos, padrões: thumbnail+título+formato+retenção), roteiro estendido (1.5k-3k palavras), comparador de nichos, agenda 4 semanas, thumbnail IA (DALL-E prompt pattern), monitoramento (watched_metrics_history), títulos alternativos (5 variações com score), favoritos/pastas, ideia do dia, exportações.
- Skill serve para reutilizar o PROCESSO (pesquisa + análise + entregáveis), não o código da aplicação web.

## Arquivos-chave
- server/exportPdf.ts: buildFavoritesPdf (linha 210+), COLORS dark/amber/gray/light/cardBg, fonte Helvetica.
- server/exportPdf.test.ts: teste de buildFavoritesPdf já existe (servidor local HTTP antesAll/afterAll, TINY_PNG base64). Adicionar/assertar título "SUGESTÃO ASSOCIADA" no output.
- client/src/pages/Favorites.tsx: countInFolder (linha ~270), handleBatchMove, selectAllFiltered, selectedIds.
- client/src/lib/export.ts: exportFavoritesPdf.

## Status final (fase 4)
- [x] 2.1 Títulos no PDF: "SUGESTÃO ASSOCIADA" (âmbar) + título destacado abaixo do card em buildFavoritesPdf (exportPdf.ts). Testes: 59/59 passando (pdf já testa com servidor local). TS ok.
- [x] 2.2 Contador por pasta: já é reativo (countInFolder deriva de items do cache; otimista updates em moveMutation atualizam o cache) — confirmado sem mudanças necessárias.
- [x] 2.3 Ctrl+A: useEffect após definição de `filtered` (linha ~359), ignora INPUT/TEXTAREA, alterna seleção do filtro atual; dica "Ctrl+A para tudo" aparece na barra de ações. TS ok.
- [x] 3 Skill: SKILL.md reescrito (7 passos, extensões alternativas/thumbnails/outline/monitoring), nova referência references/features-roadmap.md; validada com quick_validate.py (valid).
- [x] 4 Testes: 59/59. Falta: screenshot + checkpoint.

## Padrões do projeto
- Testes: pnpm test (vitest), 59 testes passando antes desta rodada.
- Checkpoints salvos a cada rodada; auto-publish ativo.

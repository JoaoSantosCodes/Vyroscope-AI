# Notas — Rodada 7

## Requisitos do usuário
1. Botão "Gerar esboço de roteiro" no painel Ideia do dia (Home.tsx → IdeaOfTheDayCard) — esboço automático a partir da sugestão do dia.
2. Drag-and-drop de thumbnails entre pastas na galeria de Favoritos (Favorites.tsx).
3. Tooltip nos growth cards do /monitorar (Monitoring.tsx → GrowthCard) com números exatos usados no cálculo %.

## Estado atual do projeto
- TS limpo, 49 testes passando (watched.test 8, extended.test 21, analysis.test 16, exportPdf.test 3, auth.logout.test 1).
- Último checkpoint: ebab020a (rodada 6), auto-publish ativo.
- extended.ts: `generateExtendedScript(niche, suggestion, patterns)` usa invokeLLM com SCRIPT_SCHEMA, valida JSON, retorna ExtendedScript {title, totalLength, fullScript, sections, notes}. Lança "llm_empty_response"/"llm_invalid_json"/"llm_invalid_structure".
- extended router: `ideaOfTheDay` retorna {idea: {niche, analysisId, analysisDate, suggestion, date}, reason} ou {idea: null, reason: "no_completed_analyses"|"no_suggestions"}.
- Watching router: watched.metrics retorna {history[], daily[], growth: {viewsPercent, likesPercent, lastWeekAvgViews, lastWeekAvgLikes, prevWeekAvgViews, prevWeekAvgLikes} | null}.
- Favorites.tsx: pills de pasta (Todas + por pasta), dialogs Nova pasta/Renomear/Excluir/Mover (dropdown de 3 pontos por card), moveMutation (extended.moveThumbnail {thumbnailId, folderId|null}).
- Home.tsx IdeaOfTheDayCard: já tem botões "Abrir análise completa" (navigate /resultado/:id) e "Copiar título + hook". ideaQuery = trpc.extended.ideaOfTheDay.useQuery.

## Progresso atual (round 7)
- [x] Backend: generateOutline(niche, suggestion, patterns) adicionado a server/extended.ts (OUTLINE_SCHEMA: {totalLength, acts[3]:{act,label,duration,points,keyLine}, notes}, valida JSON, lança llm_*).
- [x] Router: extended.generateIdeaOutline mutation (sem input, reutiliza lógica de ideia do dia, retorna {niche, analysisId, suggestion, outline}). TS limpo.
- [x] Home.tsx: OutlineDialog criado + botão "Gerar esboço de roteiro" no IdeaOfTheDayCard (variant secondary, disabled isPending, Loader2 spinner) + mutation onSuccess setOutline+open. Import FileText e Dialog adicionados. TS limpo.
- [x] Favorites.tsx: drag-and-drop completo (draggable nos cards, drop targets nas pills Todas/pastas, destaque visual, toast com destino).
- [x] Monitoring.tsx: GrowthCard com tooltip de hover "Números do cálculo" (médias diária exatas e fórmula) + title nativo com detalhe.
- [x] Testes: 3 testes de generateIdeaOutline adicionados; 52/52 passando.
- [x] Screenshots OK (home com Ideia do dia, favoritos com pastas, monitorar).

## Plano de implementação
- Backend: `generateOutline` (extended.ts) — reutiliza fluxo de generateExtendedScript mas com schema menor (outline: título, hook, abertura em 3 blocos: abertura/desenvolvimento/fechamento com tópicos e falas-chave, notas) — texto curto (~300-500 palavras).
- Router: `extended.generateIdeaOutline` protected mutation, usa ideaOfTheDay lógica (analyses concluídas do usuário, niche principal, sugestão top do dia) + generateOutline.
- Frontend Home: botão "Gerar esboço de roteiro" → modal com loading, resultado (blocos), copiar texto + exportar TXT.
- Frontend Favorites: HTML5 DnD — card thumbnail como draggable (draggable=true, onDragStart set dataTransfer com thumbnailId), pasta pill como drop target (onDragOver preventDefault, onDrop → moveMutation), drop visual feedback (state draggedOver).
- Frontend Monitoring: tooltip shadcn/ui Tooltip envolvendo GrowthCard com texto detalhado (ex: "médias: 3 leituras na última semana → média 400 views; semana anterior 450 views → -11%").
- Testes: adicionar caso ao extended.test (mock invokeLLM + db) e verificar mocks existentes.

## Arquivos-chave
- server/extended.ts (funções)
- server/routers/extended.ts (procedures; listFolders ~linha 175, ideaOfTheDay ~227)
- client/src/pages/Home.tsx (IdeaOfTheDayCard ~linha 222)
- client/src/pages/Favorites.tsx
- client/src/pages/Monitoring.tsx (GrowthCard ao final do arquivo)
- client/src/components/ui/tooltip.tsx (já existe)

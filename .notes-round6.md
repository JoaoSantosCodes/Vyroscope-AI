# Rodada 6 — Progresso

## Requisitos do usuário (mensagem mais recente)
1. Gráfico do monitoramento: médias diárias + indicador de crescimento % vs semana anterior.
2. Favoritos: pastas para organizar thumbnails por projeto/canal.
3. Home: painel "Ideia do dia" com sugestões automáticas do nicho principal do usuário.

## FEITO (backend)
- Schema: coluna `folderId` em `suggestion_thumbnails` + tabela `thumbnail_folders` (id, userId, name, color, createdAt). Migration 0005 aplicada (CREATE TABLE + ALTER ADD folderId) via webdev_execute_sql. Snapshot 0005 já contém folderId.
- db.ts: helpers `createThumbnailFolder`, `listThumbnailFolders`, `updateThumbnailFolder`, `deleteThumbnailFolder`, `moveThumbnailToFolder`. Import reorganizado: `suggestionThumbnails as stCols` + `thumbnailFolders` (linha ~209). Erros TS corrigidos.
- watched.metrics agora retorna `daily[]` (médias por dia agregando pontos) e `growth` (viewsPercent/likesPercent vs semana anterior; lastWeekAvgViews/Likes, prevWeekAvgViews/Likes; prev=0 → null se curr=0, senão 100).
- extended router: `listFolders`, `createFolder` (name, color opt), `updateFolder` (folderId, name?, color nullable), `deleteFolder`, `moveThumbnail` (folderId nullable).

## ATUALIZAÇÃO
- `extended.ideaOfTheDay` pronto (nicho principal = mais analisado, rotação determinística por dia; retorna {idea: {niche, analysisId, analysisDate, suggestion, date}, reason} ou {idea: null, reason} se sem análises). `listAnalysesByUser` agora inclui `result` no select. TS OK, 40 testes passando.

## FRONTEND CONCLUÍDO
- Monitoring.tsx: seletor "Todas as leituras"/"Médias diárias" (granularity), charts usam seriesData (daily do backend quando selecionado), cards GrowthCard com viewsPercent/likesPercent vs semana anterior + médias da semana.
- Favorites.tsx: reescrita — pills de pasta (Todas + por pasta com contadores), hover em pasta → menu renomear/excluir, botão Nova pasta (Dialog com cor), cards com menu de 3 pontos (Mover para pasta, Remover da pasta, Baixar PNG, Remover favoritos), badge de pasta no card, estados vazios distintos.
- Home.tsx: IdeaOfTheDayCard após login (condicional no hero): isLoading skeleton, no_completed_analyses com CTA, ideia com título, score badge (scoreColor/scoreLabel), hook blockquote, ângulo, botões "Abrir análise completa" (navigate /resultado/:id) e "Copiar título + hook", botão Trocar ideia (invalida cache).
- TS limpo. Falta: testes vitest (mocks ideaOfTheDay, folders, growth/daily), screenshots, checkpoint.

## PENDENTE
- Backend: procedure `ideaOfTheDay` (extended.router) — nicho principal = análise mais frequente (ou mais recente concluída) do usuário; retorna uma sugestão do dia: escolher sugestão de uma análise completed recente + rotação por dia (hash da data). Formato de retorno: { niche, suggestion, analysisId, suggestionIndex, date }. Se sem análises concluídas → null com mensagem.
- Frontend Monitoring.tsx: usar daily + growth. Gráfico com médias diárias (quando daily.length > 0, senão fallback history dedup). Adicionar cards de crescimento (↑/↓ X% views e likes vs semana anterior) + tooltip com lastWeekAvg/prevWeekAvg. Usar seletor existente (metric: views|likes).
- Frontend Favorites.tsx: sidebar/tabs de pastas, botão criar pasta (Dialog), renomear (DropdownMenu ou dialog), excluir (AlertDialog), menu "Mover para pasta" (DropdownMenu) em cada card, contador por pasta, mostrar pasta no card.
- Frontend Home.tsx: painel "Ideia do dia" (após login) — usar trpc.extended.ideaOfTheDay.useQuery(); card com título, hook, score, botões: geram roteiro (open dialog — pode redirecionar à análise? melhor: botão "Abrir análise" link /resultado/:id) e "Gerar thumbnail". Mostrar também data de hoje e botão "Nova ideia" (invalidar).
- Testes: atualizar mocks (watched.metrics agora tem daily/growth; testar crescimento; folder CRUD; ideaOfTheDay). Rodar pnpm test → passar tudo.
- Screenshots: /monitorar (com dados), /favoritos (com pasta), / (ideia do dia). Checkpoint.
- auto-publish ativo: vyrosai-9gwgsgfg.manus.space.

## Detalhes úteis de arquivos
- Monitoring.tsx: componente VideoRow com VideoChart (Recharts LineChart), seletor "Evolução". Hook watched.metrics.useQuery({id}) existe; dedupe de history por hora no client — substituir uso por `data?.daily` se houver.
- Favorites.tsx: usa listFavorites (join analyses+suggestion_thumbnails), cada card tem dropdown (download, deselect). Adicionar pasta.
- Home.tsx: landing page; perfil logado disponível via useAuth() (client/src/_core/hooks/useAuth.ts). Router: /resultado/:id, /favoritos, /monitorar, /comparador, /perfil, /historico.
- SiteLayout nav: Análise, Comparador, Monitorar, Histórico, Favoritos (client/src/components/SiteLayout.tsx).
- Tipos AnalysisResult: result.suggestions (Suggestion: title, hook, angle, narrativeStructure, targetLength, viralityScore, reasoning), patterns (ViralityPattern).
- Screenshot path de análise: /resultado/gtdZqHAdbN1VNS (porém análise failed — YouTube).
